/****************************************************
 * @file    03_sequence_containers.cc
 * @brief   序列容器(sequence_containers: array,linked list)
 *          vector,deque,list,forward list,array
 *
 *          关联容器(associative containers: binary tree)
 *          set,multiset,map,multimap 
 *          核心特性是元素始终保持有序
 *
 *          无序容器(unordered containers: hash table)
 *          unordered set/multiset
 *          unordered map/multimap
 *          核心特性是元素是无序的
 * @author  bitofux
 * @date    2025-08-17
 ****************************************************/
#include <iostream>
#include <vector>
#include <deque>
#include <list>
#include <set> //set and multiset
#include <map> //map and multimap
#include <unordered_set> //unordered set/multiset
#include <unordered_map> //unordered map/multimap
#include <iterator>
#include <algorithm> //some algorithm
#include <numeric> //some numeric algotithm
#include <functional> //functor
